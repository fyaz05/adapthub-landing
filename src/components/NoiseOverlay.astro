<svg class="pointer-events-none fixed isolate z-max opacity-[0.03] mix-blend-overlay w-full h-full top-0 left-0 hidden md:block">
<defs>
    <filter id="noiseFilter">
      <feTurbulence
        type="fractalNoise"
        baseFrequency="0.8"
        numOctaves="3"
        stitchTiles="stitch"></feTurbulence>
    </filter>
    <pattern id="noisePattern" width="128" height="128" patternUnits="userSpaceOnUse">
      <rect width="128" height="128" filter="url(#noiseFilter)"></rect>
    </pattern>
  </defs>
  <rect width="100%" height="100%" fill="url(#noisePattern)"></rect>
</svg>

<style>
  svg {
    position: fixed;
    top: 0;
    left: 0;
    width: 100vw;
    height: 100vh;
    pointer-events: none;
  }
</style>
