# Cloudflare Pages Caching Rules

# Astro assets have cache-busting hashes, safe to cache forever
/_astro/*
  Cache-Control: public, max-age=31536000, immutable

# All other static files (like HTML and unhashed public/ assets) should revalidate
/*
  Cache-Control: public, max-age=0, must-revalidate
